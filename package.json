{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.2.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2"
  }
}
