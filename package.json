{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.4.2",
    "purescript": "^0.12.5",
    "purescript-psa": "^0.7.3",
    "rimraf": "^2.6.3"
  }
}
